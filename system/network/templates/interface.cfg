auto {{ item.key }}

iface {{ item.key }} inet static
	address {{ item.value.address }}
{% if item.value.gateway is defined %}
	gateway {{ item.value.gateway }}
{% endif %}

{% for alias in item.value.aliases | default %}
iface {{ item.key }} inet static
	address {{ alias }}
{% endfor %}
