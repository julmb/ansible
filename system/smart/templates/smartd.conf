{% set td = temperature.difference | default(0) %}
{% set ti = temperature.information | default(0) %}
{% set tw = temperature.warning | default(0) %}
DEVICESCAN -a -n standby -s {{ schedule }} -W {{ td }},{{ ti }},{{ tw }} -m @notify.py
