- name: "{{ item.volume.name }} definition"
  ansible.builtin.command: virsh vol-create-as {{ item.volume.pool }} {{ item.volume.name }} {{ item.volume.size }}
  register: result
  failed_when: result.rc != 0 and 'exists already' not in result.stderr
  changed_when: result.rc == 0
