<volume type="file">
  <name>{{ volume.key }}</name>
  <capacity unit="{{ volume.value.capacity.unit }}">{{ volume.value.capacity.value }}</capacity>
  <target>
    <format type="{{ volume.value.format }}"/>
  </target>
</volume>
