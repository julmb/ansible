<domain type="{{ item.value.type }}">
  <name>{{ item.key }}</name>
  <uuid>{{ item.key | to_uuid }}</uuid>
  <vcpu>{{ item.value.cpus }}</vcpu>
  <memory unit="bytes">{{ item.value.memory }}</memory>
  <os>
    <type arch="{{ item.value.architecture }}" machine="{{ item.value.chipset }}">hvm</type>
    <loader readonly="yes" type="pflash">/usr/share/OVMF/OVMF_CODE_4M.fd</loader>
  </os>
  <features>
    <acpi/>
  </features>
  <cpu mode="host-passthrough" />
  <devices>
    <emulator>/usr/bin/qemu-system-x86_64</emulator>
{% for entry in item.value.storage %}
    <disk type="{{ entry.meta.type }}" device="{{ entry.meta.kind }}">
{% if entry.meta.type == 'volume' %}
      <source pool="{{ entry.content.pool }}" volume="{{ entry.content.volume }}"/>
{% elif entry.meta.type == 'file' %}
      <source file="{{ entry.content }}"/>
{% endif %}
      <target dev="{{ entry.meta.device }}" bus="{{ entry.meta.bus }}"/>
      <boot order="{{ entry.meta.boot }}"/>
    </disk>
{% endfor %}
    <interface type="network">
      <mac address="52:54:00:8e:d6:bb"/>
      <model type="virtio"/>
      <source network="default"/>
    </interface>
    <graphics type="spice" autoport="yes" />
    <video>
      <model type="virtio" />
    </video>
  </devices>
</domain>
